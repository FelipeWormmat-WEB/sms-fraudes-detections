# Copy to .env (do not commit .env). Prefer root .env when using docker-compose.

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# Must match gateway-service INTERNAL_SERVICE_TOKEN when calling /analyze
INTERNAL_SERVICE_TOKEN=
